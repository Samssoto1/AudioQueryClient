export class Question{
    constructor(
        questionTitle: string,
        answers: Array<string>,
        correctAnswer: Object, 
        quizId: string,
        songId: string
        ){
    }
}